# carinfo-bot — Project Instructions

## Pull Requests

**Always open a new PR after every push to remote.**

After every `git push`, immediately create a PR using the GitHub MCP tool (`mcp__github__create_pull_request`) targeting `main`. Do not wait for the user to ask.
